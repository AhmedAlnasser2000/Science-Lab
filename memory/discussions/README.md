# Discussions Lane

This lane stores long-form design talks and explorations.

Rules:
- non-binding by default
- no auto-promotion
- only explicit `DISCUSSION APPROVE` (or explicit `MEMORY PROMOTE`) creates canonical records

See `memory/discussions/INDEX.md` and `memory/discussions/templates/discussion-template.md`.
